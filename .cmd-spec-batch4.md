# MIST Landing — Batch 4 (event photos + 4 more tweet embeds + copy polish)

Repo: /home/shzzmoi/Projects/landing — Next.js 15 App Router + React 19 + Tailwind 4 + TS, pnpm. Design system applied. Small steps, verify `pnpm build`, DO NOT commit.

## Context / source material
- Event photos live in `/home/shzzmoi/Documents/MistMedia/` — only the `events-*` speaking/talk shots are to be used (user: "only the ones where I'm speaking at the events"). The StarkSpace/sofa/selfie/sticker/family/group photos are social/community — do NOT use them.
- Event photos available (all valid JPEGs): `events-ig-ba-ecc.jpeg` (1600×1200), `events-kzg-whiteboard-session.jpeg` (1600×1200), `events-muchiangmai-account-abstraction.jpeg` (2048×1536), `events-zuafrique-zk-primitives.jpeg` (2048×1365), `events-zugrama-panel.jpeg` (2048×917, wide), `events-zugrama-zk-state.jpeg` (2048×917, wide).
- The `.commandcode/taste` guidance: don't fabricate detail about what images show; flag ambiguity. The user selected these as "speaking at events" images, so use them as talk/event credibility shots. The wide 917-height ones (zugrama-panel, zugrama-zk-state) read as panels/talks; the others are event photos. If an image is genuinely unclear as a talk shot, still use it in the events context (user chose them) but keep alt text generic ("Shramee speaking at [event]") — do NOT invent event names not in the filename.

## 1. Copy: always speak to the USER, never to the site owner (Shramee)

The homepage and team page must address the visitor (the protocol builder / integrator / reader), not read like MIST talking to itself. Fix second-person that reads as "talking to Shramee":

- `src/components/landing/CTASection.tsx` default body: "Integrate private settlement into **your** protocol. The docs specify the full operator and integrator path." → change "your protocol" to "any protocol" (speaks to the reader building a protocol, universal). New: "Integrate private settlement into any protocol. The docs specify the full operator and integrator path." Keep the rest.
- `src/app/team/page.tsx`:
  - Section heading "Every credential, and what it **buys you**." → this "you" is ambiguous/fishy (reads as Shramee talking to himself). Change to something visitor-facing: "What each credential means." or "The record, and what it means." (third person, about the founder).
  - Sweep the team page for any other "you/your" that addresses the visitor as if they were Shramee — the page should describe Shramee in third person throughout. (Contact CTAs like "Email Shramee" are fine — they address the visitor by telling them what to do, that's correct.)
- Homepage: scan all homepage components (HeroSection, ProtocolShowcase, ArchitectureSection, ProofPointsSection, CompetitiveTable, BlogPreview, SignalSection) for "you/your" that addresses the reader as the site owner. The brand line "Privacy infrastructure by your rules" in Hero/Footer is INTENTIONAL — keep it. Any other "you/your" on the homepage should be either (a) genuinely addressing the visitor as a protocol builder (keep), or (b) fishy/self-addressed (rewrite to third person or "any"). Use judgment; when in doubt, prefer third-person/universal phrasing on the homepage.
- The partners page KEEPS its "you" — it's a B2B page addressing the partner (correct, do not change).

## 2. Team page: largely a few paragraphs, except the story

Current team page has a 7-card credential grid. User wants it "largely a few paragraphs. Except how MIST started." Restructure:

- Keep the hero (intro + portrait/placeholder).
- Replace the 7-card credential grid with **2–4 flowing paragraphs** that weave the credentials into prose (third person, mechanism-then-payoff voice). E.g. paragraph 1: 12 years experience + full-time dev since dropping out + lecturer at PaCE@NTU; paragraph 2: ZK credentials (first proof on Starknet, Gnark largest external contributor, Aztec/World ProveKit/Arkworks contributions); paragraph 3: Singapore government work → why auditability/compliance is built in. Write it as a bio a partner would read and trust. No card grid, no bullet list of credentials.
- KEEP the "How MIST started" story timeline (the exception — user wants it kept). Keep the story section as-is (timeline of near-miss → Cairo → Ben-Sasson → first proof).
- Keep contact section.
- Optionally: add ONE event photo (see item 3) to the team page hero or story section if it fits naturally — a speaking shot adds credibility. Use a white card + Misty border + 2px radius framing (design system: bordered cards, no full-bleed photos).

## 3. Event photos into the site

Copy the chosen event photos into `public/` (e.g. `public/events/`), and use them where they support positioning (speaking at events = credibility for a protocol founder). Recommended placements:
- **Homepage**: add an events/talks strip or integrate into the Signal/"In the wild" section area — e.g. a small "Speaking" or event-credibility band showing 1–2 event photos (zugrama-panel wide + one other) with a caption line like "Speaking on ZK privacy at [event] — 2024/2025". Keep it small, bordered white cards, on-brand. OR add to the team page only if homepage stays clean — your judgment, but the user wants them "in our designs," so at least one prominent placement (team page hero or homepage events band).
- **Team page**: the founder speaking at events supports "built by one founder who ships" — add 1–2 event photos (e.g. `events-zugrama-panel` and `events-zuafrique-zk-primitives`) framed as bordered cards.
- Image handling: copy files to `public/events/`, use `next/image` with proper width/height (no layout shift), alt text like "Shramee speaking at a ZK privacy event" (do NOT invent specific event names unless the filename implies it — zugrama/zuafrique/muchiangmai/ig-ba-ecc are location/org hints; keep alt generic or use the hint as-is like "ZK event in Zug" only if confident — otherwise generic).
- Keep design system: white cards, 1px Misty border, 2px radius, no shadows, no full-bleed, no gradients over photos.

## 4. Add 4 more tweet embeds to the Signal section

Verified public tweets (all render; all StarkWare/Starknet-Foundation-adjacent, strong on-positioning):
1. Akash (@akashneelesh, StarkWare): https://x.com/akashneelesh/status/2001166694572810503 — "got something that you need some crazy optimisations, call @shrameetweets" quoting the 4.3x faster Poseidon post.
2. Abdel (@AbdelStark, StarkWare): https://x.com/AbdelStark/status/1992295027394146593 — "real life privacy payment on starknet via mist cash protocol, today, on mainnet."
3. Jonatan Chaverri (@jonatanChaverri): https://x.com/jonatanChaverri/status/1992255142939472293 — "proof that you can integrate privacy into your app in less than 24 hours with Mist Cash."
4. David Barreto (@barretodavid, Starknet Foundation): https://x.com/barretodavid/status/1992238899989770648 — CofiBlocks integration, coffee producers safer via private payments.

Add these to the Signal section (SignalSection.tsx) alongside the existing 3 (Kubjas, LeViness, Laura). The section becomes 7 tweets. Layout: the current grid — check how SignalSection lays out tweets; if it's a 2-col grid, 7 tweets leaves an odd cell (per earlier grid concern). Options: keep grid and let the 7th span/center, OR make it 3-col × 2 rows + 1, OR reduce visible tweets (pick the strongest). User earlier said "less is better than made up" for grids — so do NOT pad. Best: arrange 7 tweets in a clean grid (e.g. `sm:grid-cols-2 lg:grid-cols-3` with the last one spanning or centered, no empty cell) OR curate to 6 strongest if 7 breaks the layout. Recommend: try 3-col with 7th spanning full width or centered; if awkward, drop to 6 (drop the weakest — likely Laura or Jonatan). Use judgment, keep it clean, no empty cells.

Keep the existing tweet CSS (design-matched, quoted-tweet cap). Verify all 7 render at build.

## Process
- Small steps, `pnpm build` after major changes, `pnpm lint` at end.
- DO NOT commit.
- Report: what changed per item, build/lint status, which tweets rendered, which event photos used + where, any copy rewrites, deviations.

## Source docs (read-only)
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Brand/MIST_and_founder_story.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Business/pitch-deck.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST Design System/SKILL.md + readme.md
