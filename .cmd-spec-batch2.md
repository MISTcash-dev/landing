# MIST Landing — Fix Batch 2 (review-driven)

Repo: /home/shzzmoi/Projects/landing — Next.js 15 App Router + React 19 + Tailwind 4 + TS, pnpm. Design system already applied. Work in small steps, verify `pnpm build` after major changes, DO NOT commit.

## 1. Partner page (separate route)

Create `/partners` — a partner-facing page for the business audience: payment service providers, OTC desks, payroll platforms, treasury teams, compliance/KYC providers. Do NOT fold partner content into the homepage.

- Route: `src/app/partners/page.tsx` + its own metadata (title "Partners — MIST", description targeting fintechs).
- Reuse the existing Navbar + Footer. Add "Partners" to the navbar nav links.
- Content from `MIST/Brand/partner-landing-page-copy.md` (the partner copy doc — PSP/OTC/payroll/treasury + compliance providers, the "Two ways to build with MIST" framing, how-it-works Chamber/Reserve/middleware, Why now, Pilot Program section).
- Pilot Program CTA is the page's primary CTA: "Apply for the Pilot Program" → mailto:shramee@MIST.cash (or a /pilot form route if trivial; mailto is fine).
- Same design system: Baby Blue bg, Ink text, Josefin headings / Sen body, 2px radius, solid-fill buttons, one cobalt CTA per view, no shadows/gradients/glows.
- Voice: mechanism-then-payoff, numbers not adjectives, no banned words (mixer/anonymous-headline/untraceable/shielded/unlock/empower/game-changing/seamless/revolutionary). No exclamation marks.
- Keep it sharp, not a wall of text. Section list roughly: hero (partner headline + Pilot CTA), the problem for financial institutions (confidential settlement + auditability, NOT fear-mongering), "Two ways to build with MIST" (for payment providers/OTC/payroll/treasury; for compliance/KYC providers — the Reserve recurring-revenue angle), how it works briefly (Chamber/Reserves/Middleware), why now (proof points), Pilot Program details + CTA, contact.

## 2. Chain list — make it CCTP/USDC-accurate

The current homepage states "6 chains: Ethereum, Base, Arbitrum, Optimism, Polygon, StarkNet" in two places (hero stat "6 chains supported" and the ProofPoints section "6 chains" card). This is wrong/stale. The real picture:

- The new cross-chain implementation works on top of **CCTP (Circle's Cross-Chain Transfer Protocol)** and supports **USDC**, on **all chains CCTP supports**. It is **currently limited to USDC** (not arbitrary assets).
- The old/legacy Starknet implementation ran over **6 specified networks** (the old chain set).
- **USDT support via usdt0 is planned down the road** (not yet).

Update the homepage copy to state this accurately and honestly:
- Hero stat: change "6 chains supported" to something accurate — e.g. "USDC via CCTP" or "CCTP networks" (do NOT claim a specific chain count that's stale). Best: a stat like "USDC across CCTP networks" with a supporting line, or drop the count and say "Cross-chain USDC via CCTP".
- ProofPoints "6 chains" card: replace with the CCTP/USDC story — e.g. "USDC via CCTP" (cross-chain USDC on all CCTP-supported networks), and optionally a second card "USDT via usdt0 — planned".
- Add an honest note somewhere appropriate (architecture or proof-points section): "Cross-chain support today is USDC over CCTP on all supported networks. The legacy Starknet deployment covered six specified networks. USDT over usdt0 is on the roadmap." — frame as scope decision, per voice rules (name the limits).
- Check any other place on the site that lists chains (CompetitiveTable? try-hidemi?) and update those too if present.

## 3. "100k+ anonymity set" reframe

The hero stat "100k+ anonymity set" and the ProofPoints "100k+ Anonymity set" card claim a current figure that is not real (protocol not live, set is 0). Replace with an honest, useful framing:
- Hero stat: replace "100k+ / anonymity set" with a better stat — suggest "~1s / proof on iPhone" already exists; use something like "USDC / via CCTP" or "ZK / recursive proofs" or "Composable / middleware rules". Pick a stat that is true and useful. (Do not invent numbers.)
- ProofPoints card "100k+ Anonymity set": reframe to describe the design goal without a live number, e.g. "Shared anonymity set — designed so users across chains draw from one set, not a pool per chain. Size grows with usage; not live yet." Or drop the card and replace with the USDT-planned card. Use judgment, keep it honest.
- Ensure no "100k+" claim remains as a current figure.

## 4. "Mixers" → "Privacy pools"

In the CompetitiveTable component, the column header "Mixers" must be renamed. Per brand audit, "mixer" is Tornado Cash's word — regulatory-coded. Rename the column to "Privacy pools". Keep the rest of the table as-is. (Also scan for the word "mixer" anywhere else in src/ and replace with "privacy pools" or remove.)

## 5. Grid empty-cell fixes

The review noted empty gray cells in the 5-card "Built for protocols" grid (2x3) and the 2-post blog grid. User's direction: LESS content is better than made-up content to match a layout. So do NOT add a 6th card or 3rd post. Instead, fix the CSS/grid so odd counts don't render an empty placeholder:
- In the protocol showcase grid (ProtocolShowcase.tsx) and blog grid (BlogPreview.tsx / blog list page), ensure the grid uses `grid-cols-2` (or similar) with `gap-px bg-misty border border-misty` pattern such that an odd number of items doesn't leave a trailing empty cell. Simplest robust fix: change grid so items fill only as many cells as exist (e.g. use `grid-template-columns: repeat(auto-fit, minmax(...))` or make the last row span, or just reduce to `grid-cols-1 sm:grid-cols-2` with only 2 items and no phantom cell). Verify visually/build. Do not add placeholder content.

## 6. Social proof section (embed tweets)

Add a "Signal" / "In the wild" social proof section to the homepage (between Blog preview and CTA, or after Proof points — your judgment), embedding 2–3 real tweets:

- Ivo Kubjas: https://x.com/ikubjas/status/2017258450435142037
- Ian LeViness: https://x.com/ExpatCrypto3/status/2017013736502866210
- Laura M González: https://x.com/LauraM10g/status/2017020644928422262

Implementation:
- Re-add `react-tweet` (it was removed earlier): `pnpm add react-tweet`. It fetches tweet data server-side and renders. Create a `TweetEmbed` component wrapping `react-tweet`'s `Tweet` / `EmbeddedTweet` with the tweet ID, styled to the design system (white card, 1px Misty border, 2px radius, Sen). Handle the suspended/unavailable case gracefully (fallback text, no crash).
- The tweets render their own content/media; do not restyle internals heavily — just container styling (card, border, radius). One cobalt CTA rule still applies (this section has no button).
- Section heading: "Signal from the field" or "In the wild" — keep on-brand (mechanism-then-payoff tone).
- If a tweet fails to render at build time (private/suspended), drop it and keep the others — note it in the report. Kubjas and LeViness verified public; Laura assumed public.

## 7. CTA consolidation (light touch)

- "Read the docs" appears in nav, hero, and bottom CTA banner. Keep nav + hero, but in the bottom CTA section consider making it "Read the docs" + keep "Contact the team" mailto (or make bottom CTA primary "Build with MIST" → /partners? No — keep homepage CTAs about docs/Hidemi; the partner page has the Pilot CTA). Minimal change: ensure the homepage has ONE primary cobalt CTA per view (nav counts separately from hero/banner per design system "one cobalt CTA per view" — keep nav's). Just don't add more CTAs; if the bottom banner currently has two cobalt buttons, demote one to secondary. Verify visually.

## Process
- Small steps, `pnpm build` after major changes, fix errors. `pnpm lint` at end.
- DO NOT commit.
- Report: what changed per item, build/lint status, any tweets that failed to render, any deviations.

## Source docs (in Obsidian vault, read-only reference)
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Brand/partner-landing-page-copy.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Brand/MIST.cash competitive brand audit.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Business/pitch-deck.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Product/ICP and early buyer research.md
