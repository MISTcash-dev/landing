# MIST Landing — Batch 3 (nav/footer refactor, team page, tweet styles, logo color)

Repo: /home/shzzmoi/Projects/landing — Next.js 15 App Router + React 19 + Tailwind 4 + TS, pnpm. Design system already applied. Work in small steps, verify `pnpm build` after major changes, DO NOT commit.

## 1. Navbar: page links only, no section links

Current `src/components/landing/Navbar.tsx` navLinks:
```
{ label: "Product", href: "#product" },
{ label: "Architecture", href: "#architecture" },
{ label: "Partners", href: "/partners" },
{ label: "Blog", href: "/blog" },
{ label: "Contact", href: "#contact" },
```
Replace section links (`#product`, `#architecture`, `#contact`) with page links. The site now has these pages: Home (/), Partners (/partners), Blog (/blog), Try Hidemi (/try-hidemi), and a new Team page (/team, see item 3). Nav should be:
```
{ label: "Partners", href: "/partners" },
{ label: "Blog", href: "/blog" },
{ label: "Team", href: "/team" },
```
(Keep "Read the docs" cobalt CTA button. Keep mobile menu behavior. Keep the logo link to /.)

## 2. Footer: same treatment + reuse

- `src/components/landing/Footer.tsx` footerLinks currently has `#product` and `#architecture` section links. Replace with page links: Partners (/partners), Blog (/blog), Team (/team), Try Hidemi (/try-hidemi). Keep contact column (mailto, Telegram, X). Keep the `id="contact"` on the footer if any page anchors to it — actually, since nav no longer links to #contact, that's fine to keep as-is (harmless).
- **Rename Navbar → Header** and **Footer stays Footer** (or keep the filenames but treat them as the shared components). The user said "re-use separate Header, Footer section components across all pages" — the point is they must be SHARED, not duplicated per page. Current state: each page imports `<Navbar />` and `<Footer />` individually (page.tsx, partners, blog, blog/[slug], try-hidemi). Refactor so Header + Footer render ONCE in the root layout (`src/app/layout.tsx`), wrapping `{children}`, and REMOVE the per-page imports. This ensures one shared Header/Footer across all pages.
  - Rename `Navbar.tsx` → `Header.tsx` (export `Header`), update all imports. Delete `Navbar.tsx`.
  - Keep Footer as `Footer.tsx` but ensure it's only imported in layout.tsx.
  - The skip-nav link stays in layout.tsx body. `<main id="main-content">` — pages currently each wrap content in `<main id="main-content">`. If layout renders Header/Footer around children, the `<main>` should be in the layout too (once), and pages should just return their sections (no per-page `<main>` wrapper). Restructure carefully:
    - layout.tsx: `<body><a skip-nav/><Header/><main id="main-content">{children}</main><Footer/></body>`
    - Each page: remove `<Navbar/>`, `<main id="main-content">`, `</main>`, `<Footer/>` wrappers; return only their content sections. For pages that currently have `<main className="min-h-screen">` (blog, partners), the min-height can move to the layout's main or stay minimal.
  - Verify build after this refactor.

## 3. Team page — introduce the founder

Create `src/app/team/page.tsx` — introduces Shramee (the founder). Reuse shared Header/Footer (now in layout). Content from the pitch deck's founder section (`MIST/Business/pitch-deck.md` in the vault):
- Shramee, The Founder:
  - 12 years of technical experience
  - Dropped out of high school to continue as a full-time dev
  - First ZK proof on Starknet; led multiple high-impact projects
  - Largest external contributor to Gnark (powers Linea crypto)
  - Contributions to Aztec, World ProveKit & Arkworks Algebra
  - Rendered multiple lectures on software dev at PaCE@NTU
  - Worked directly with the government of Singapore
- Add the founder story hook (from MIST_and_founder_story.md): the August 2023 Singapore solvency near-miss that started MIST, the Cairo turn (HE Nov 2023), Eli Ben-Sasson conversation (Dec 2023), first ZK proof on Starknet July 2024.
- Add contact: shramee@MIST.cash, Telegram @shramee, X @shrameetweets.
- Design: same design system. Hero-ish section (Badge "Team" or "Founder", H1 like "Built by one founder who ships." or "Shramee" — keep on-brand, Josefin heading), a founder bio section (mechanism-then-payoff: each credential followed by what it means), a "The story" section (the founder story), a contact/CTA section. No photos provided — use a clean typographic layout or a simple placeholder avatar block (initials in a white card with Misty border). Do NOT invent credentials or numbers.
- Metadata: title "Team — MIST", description introducing the founder.
- Add "Team" to nav (item 1) and footer (item 2).

## 4. Tweet styling — cap quoted-tweet height + match site style

The tweet embeds (SignalSection / TweetEmbed via react-tweet) currently render with react-tweet's default styles, which clash with the design system (rounded, shadows, different fonts). Add CSS overrides in `src/app/globals.css` to make them match:

- **Cap quoted tweet height** (user explicitly asked):
```css
[class*='quoted-tweet-container'] {
  max-height: 8em;
  overflow: hidden;
}
```
- Match site style: the tweet cards should look like the site's cards — white bg, 1px Misty Blue border, 2px radius, Sen font, Ink text, no shadows, no rounded-pill internals. Target react-tweet's classes (they use `react-tweet-theme` classes like `.react-tweet-theme`, `[class*='tweet-']`). Override in globals.css (scoped to the Signal section or the embed wrapper to avoid global bleed):
  - `.react-tweet-theme` — font-family Sen, background transparent/white, no border-radius > 2px on containers, remove box-shadows.
  - Avatar images: 2px radius (react-tweet uses rounded-full avatars — the design system is 2px everywhere; flag as intentional deviation OR override to 2px; user said match site style, so override to 2px).
  - Borders: 1px Misty for any card borders.
  - Remove any gradients/shadows in tweet internals.
  - The `TweetEmbed` figure wrapper already has `border border-misty bg-white` + 2px radius — keep that; make the inner react-tweet content transparent so the figure's border is the card edge.
- Keep the fallback text styling.

Check what react-tweet classes actually render (inspect node_modules/react-tweet or the rendered HTML) and target them precisely. Verify visually after.

## 5. Logo colors — match site style

The nav/footer use `/mist.cash.svg` (the full wordmark). The user says it renders jet black, darker than anything else on the site — recolor to match unless intentional. The site's darkest tone is Ink `#081019`; body text is Ink. The design system logo assets exist: `src/app/design-system/assets/logo.svg` (cobalt fill mark), `logo-on-cobalt.svg` (baby blue), `mist.cash.svg` (wordmark, likely black).

- The cleanest on-brand fix: the wordmark `mist.cash.svg` should be **Ink `#081019`** (not pure black #000) to match body text exactly, OR switch to the cobalt logo mark + wordmark in Ink.
- Approach: edit `public/mist.cash.svg` — the paths use implicit black fill (fill-rule:nonzero, no explicit fill on most paths, defaulting to black). Add/replace fill on the root `g` or paths to `#081019` so the whole wordmark is Ink. Verify it's not pure black after. (Check: if the SVG has `style="fill-rule:evenodd"` and no fill, add `fill="#081019"` to the outermost `<g>` or `<svg>`.)
- Also check `public/logo-mist-new.svg` and `logo-mist-on-cobalt.svg` — they're already cobalt/baby-blue (good). If any logo renders jet black and isn't supposed to, fix to Ink or the design-system color.
- The favicon/apple-touch-icon may also be black — leave those (favicons are fine), just fix the in-page logos.

## Process
- Small steps, `pnpm build` after major changes (especially the layout/Header/Footer refactor — that touches every page), fix errors. `pnpm lint` at end.
- DO NOT commit.
- Report: what changed per item, build/lint status, any react-tweet class names you discovered and targeted, logo color decisions, deviations.

## Source docs (read-only reference)
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Business/pitch-deck.md
- /home/shzzmoi/Documents/Obsidian Vault/MIST/Brand/MIST_and_founder_story.md
- Design system: /home/shzzmoi/Documents/Obsidian Vault/MIST Design System/ (SKILL.md, readme.md, assets/)
