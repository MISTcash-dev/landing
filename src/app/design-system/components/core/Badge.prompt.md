Small status label — tracked uppercase Josefin, 2px corners, no pill shape.

```jsx
<Badge tone="cobalt">Live</Badge>
```

Tones: `cobalt`, `misty`, `baby`, `ink`. Use `misty`/`baby` for neutral status, `cobalt` sparingly (it competes with the one-CTA-per-view rule).
