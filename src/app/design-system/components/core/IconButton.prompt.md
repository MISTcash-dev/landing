Square icon-only affordance for toolbars and dense rows. Always pass `label` (used as `aria-label`/`title`).

```jsx
<IconButton icon={<Icon name="x" />} label="Close" variant="secondary" />
```

Same fill rules as Button: `primary` is Cobalt, `secondary` is Misty Blue. 2px radius, no circular icon buttons.
