Labeled text field. Error state swaps the border to Cobalt (3:1+ against white, used as a UI/non-text signal here).

```jsx
<Input label="Operator ID" placeholder="acme-01" helpText="Matches your registry key" />
```
