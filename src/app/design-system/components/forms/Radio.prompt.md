Single radio input — the one deliberate exception to square geometry (a circular selection mark reads clearer than a square dot). Compose several under one `name` for a group.

```jsx
<Radio name="net" label="Mainnet" checked={net==='main'} onChange={() => setNet('main')} />
```
