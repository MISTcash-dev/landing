Square check (2px radius, never round). Checked state fills Cobalt with a white check.

```jsx
<Checkbox label="I have reviewed the compliance path" checked={ok} onChange={e => setOk(e.target.checked)} />
```
