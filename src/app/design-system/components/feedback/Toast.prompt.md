Ink-fill notification bar (no color-coded success/error system in the source guide — only ink and cobalt fills exist as system colors).

```jsx
<Toast message="Registry published to block 18,204,113" onDismiss={close} />
```
